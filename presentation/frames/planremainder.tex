\begin{frame}[t]{}
	\Huge What remains of the plan?
	\normalsize
	\begin{enumerate}
		\item \only<1>{Build a $Y$-gate standard cell library
			\begin{itemize}
				\item E.g. gates for $Y_1$, $Y_2$, $Y_3$
			\end{itemize}}
			\only<2->{\textbf{\textcolor{red}{\st{Build a $Y$-gate standard cell library}}}
			\begin{itemize}
				\item \textbf{\textcolor{red}{\st{E.g. gates for $Y_1$, $Y_2$, $Y_3$}}}
			\end{itemize}}
		\item \textbf{\textcolor{red}{\st{Use EPFL Majesty logic package to optimize}}}
			\begin{itemize}
				\item \textbf{\textcolor{red}{\st{Aggressive logic rewriting using SAT}}}
			\end{itemize}
		\item \only<1-2>{Map optimized circuit using $Y$-gate library}
			\only<3->{\textbf{\textcolor{red}{\st{Map optimized circuit using $Y$-gate library}}}}
	\end{enumerate}
	\only<3>{
		\begin{figure}[b]
			\includegraphics[height=5cm]{knighthorse}
		\end{figure}
	}
\end{frame}

